Search the TMT legal intelligence repository.

Query: $ARGUMENTS

Steps:
1. Use Grep to search across all documents in `sources/` and `summaries/`
2. Use Glob to find relevant files by name pattern
3. If needed, use WebSearch for latest online developments
4. Synthesize and present findings with file paths and relevant excerpts
